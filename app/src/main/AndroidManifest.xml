<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bernard_gu.sistemaseleccionadordeproyectos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CriteriosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCriterioActivity" />
        <activity android:name=".RegistroProyectosActivity" />
        <activity android:name=".AsignacionValoresActivity" />
        <activity android:name=".MatrizDecisionActivity" />
        <activity android:name=".PresupuestoActivity"></activity>
    </application>

</manifest>